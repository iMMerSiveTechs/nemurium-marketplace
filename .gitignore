# Raw screenshot captures are archived in outputs/site-screenshot-sources/.
# Only the optimized JPEGs the page actually references belong here.
assets/captures/
*.png
!assets/apple-touch-icon.png

# The signed DMG is a build artifact, not source. Regenerate with
# `npm run build:glassgraph-release` in work/glassgraph-studio.
downloads/*.dmg

.DS_Store
dist-site/
